import Recat from "react";
import {view ,StyleSheet,Text} from 'react-native';

const TrackScreen = () => {
    return <Text style={{fontSize:48}}>TrackScreen</Text>;
}

const styles = StyleSheet.create({});

export default TrackScreen;
 